name: Pipeline for merge into master

on:
  push:
    branches: [mock_master]

jobs:
  test:
    uses: ./.github/workflows/tests.yml
  deploy-nightly:
    needs: test
    uses: ./.github/workflows/docker-deploy.yml
    with:
      version: nightly
